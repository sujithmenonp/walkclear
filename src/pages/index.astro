---
import { getVisibleNotes } from '../lib/notes';
import Base from '../layouts/Base.astro';
import NoteCard from '../components/NoteCard.astro';
const notes = (await getVisibleNotes()).slice(0, 6);
---
<Base>
	<h1>System design</h1>
	{notes.map((note) => <NoteCard {note} />)}
</Base>
